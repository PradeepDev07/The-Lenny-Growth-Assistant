# ==============================================================================
# The Lenny Growth Assistant — Frontend Production Example (.env.production.example)
# ==============================================================================

# Public API Gateway (Inlined into client JS bundles during `npm run build`)
NEXT_PUBLIC_API_URL=https://lennygrowthapi.pradeepleadsystems.in

# Server Runtime Settings
PORT=3000
HOSTNAME=0.0.0.0
NODE_ENV=production
NEXT_TELEMETRY_DISABLED=1
